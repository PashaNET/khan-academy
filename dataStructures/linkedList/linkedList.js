class Node {
    constructor(value) {
        this.value = value
        this.next = null
    }
}

class LinkedList {
    constructor() {
        this.length = 0
        this.head = null
    }

    // methods to implement:

    // peekHead()
    // size()
    // add(value)
    // addAt(index)
    // remove(value)
    // removeAt(index)
    // indexOf(value)
    // elementAt(index)
    // isEmpty()
}